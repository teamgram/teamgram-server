<?xml version="1.0" encoding="UTF-8"?>
<table sqlname="default_history_ttl">
    <operation name="InsertOrUpdate">
        <sql>
            INSERT INTO default_history_ttl
                (user_id, period)
            VALUES
                (:user_id, :period)
            ON DUPLICATE KEY UPDATE
                period = VALUES(period)
        </sql>
    </operation>

    <operation name="Select">
        <sql>
            SELECT
                id, user_id, period
            FROM
                default_history_ttl
            WHERE
                user_id = :user_id
        </sql>
    </operation>
</table>
